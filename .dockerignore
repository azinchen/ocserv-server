# The image is built from the Dockerfile + rootfs/ only (ocserv is fetched
# during the build). Keep everything else out of the build context.
.git
.github
.gitignore
.dockerignore
.gitmodules
wiki
docs
scripts
docker-compose.yml
LICENSE
*.md
